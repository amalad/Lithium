#Comments #

func avg(int a[], int n)
{
	int i;
	int sum=0;
	for(i=0; i<n; i++)
	{
		sum=sum+a[i];
	}
	return sum/(float)n;
}

int \arr[]={1,2,3,4,5,6,7}; #Should raise an error for \#
print(avg(arr));

print("Have a nice day!!!!");


